# Core MCP server
mcp[cli]>=1.2.0

# Strategy model + features
xgboost>=2.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Free historical data for training (no credentials needed)
yfinance>=0.2.0

# Live trading only (paper mode does not need this)
robin_stocks>=3.0.0

# Dev / tests
pytest>=7.0.0
